[project]
name = "newvenue-data-engine"
version = "0.1.0"
description = "Validation-stage NSW hospitality opening signal and enrichment dataset pipeline"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]

[project.scripts]
newvenue = "newvenue.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
